# APPDevTool 待办事项

- **`TODO`: 需要完成的任务或需要进一步处理的事项。**
- **`FIXME`: 存在的 bug 或缺陷(不会导致功能上的错误)。**
- **`BUG`: 存在的 bug 或缺陷(会导致功能上的错误)。**
- **`HACK`: 临时性的解决方案或修补。**
- **`XXX`: 需要特别注意或需要进一步检查的地方(优化点)。**

## BUG 安装后 `gradle-wrapper.jar` 文件失效

## BUG 安装后 `default.keystore` 文件失效

## BUG 配置文件兼容性处理

## BUG 命令行 --project 选项拷贝项目目录出错

## TODO 增加使用虚拟机选项

## TODO 命令行上色

## TODO 输出运行应用日志

## TODO 增加功能：使用模拟器或者搜索设备：是否直接使用默认模拟器自动打开

## TODO 热重载功能，原理

## TODO 增加API 调用，用于对接 h-uni-build app 平台

## TODO 更新文档







